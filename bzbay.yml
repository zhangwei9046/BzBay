# Database settings.
database:
  driverClass: org.postgresql.jdbc.Driver
  user: root
  password: password
  url: jdbc:postgresql://localhost:5432/test

#  use the simple server factory if you only want to run on a single port
server:
    type: simple
    connector:
      type: http
      port: 8080